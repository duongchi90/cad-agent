# Confirmed Native Dimensions Plan

**Status:** Implemented; final candidate verification pending

1. Add failing tests for confirmed and unverified cross-validations.
2. Add opt-in native dimension generation and BuildResult evidence.
3. Extend Reviewer #1 to validate dimension count, handles, layers, and
   measurements.
4. Enable dimensions in the staged `cad_agent` DXF path.
5. Run focused, authoritative offline, private-data, and live Mechanical gates.
6. Record the final implementation SHA and release evidence.
